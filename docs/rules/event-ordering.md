# 同时刻顺序

先校验本批行情完整度，再处理当时生效权益和资格（资格按交易日 sellableAt 判定）；发布完成 bar；按订单接受序号共享容量撮合并结算；最后收盘 DAY 过期、登记权益和最后交易清理。每时刻一个原子提交，提交后才接收下一指令。
所有公开写入进入同一 Promise 串行队列。空闲游标移动与 checkpoint 不制造经济事件。缺数据时不提交失败批次，也不越过该批次使订单过期。
