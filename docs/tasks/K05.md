# K05 — 订单撮合

目标：保护价、冻结、共享容量提案、累计费用、FIFO 成本；依赖 K04；修改 orders/execution/fees/settlement。
实际命令：`pnpm build`、`pnpm exec vitest run packages/game-core/src/features/execution`。
结果：受控推进同一撮合器验证 200 股两次成交，总佣金 500 分、现金 9,799,500 分、冻结归零。状态 DONE。
未完成：完整时钟接线和跨日独立基准在 K06；初次测试相对导入路径错误已纠正。
