# K04 — 账户与结算基础

目标：多账户、统一钱包、凭证、批次、幂等串行入口；依赖 K03；修改 core/features、core/application、memory adapter。
预期：资金转移无损，失败无部分副作用，超安全整数无损。
实际命令：`pnpm build`、`pnpm exec vitest run tests/integration/wallet.test.ts`。
结果：1 个包含独立金额和幂等断言的集成测试通过，状态 DONE。
未完成：成交由 K05、持久化故障由 K08 验证。
