# K06 — 调度

目标：有序时间批次、自动会话、完整 bar 匹配和缺口停止；依赖 K05；修改 scheduler/event-processing/kernel。
实际命令：`pnpm build`、`pnpm exec vitest run tests/integration/clock.test.ts`。
结果：4 项测试通过：10008890 分独立买卖基准、未来不可见、缺失安全停止、逐事件/批量一致。状态 DONE。
未完成：公司行为在 K07 接到同一批处理。
