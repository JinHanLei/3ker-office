# K00 — 工程启动

- 目标：从仅有任务文件、无 Git 元数据和无远端引用的目录初始化。
- 依赖：用户 Git 身份，Node 官方下载，npm registry。
- 允许修改：工程配置、忽略规则、验证入口。
- 预期：workspace 构建及 SQLite 重开可用。
- 实际环境：Windows，原 Node 22.11.0；本轮下载官方 Node 24.14.0 到忽略目录 `.runtime/tools`，不更改系统安装。pnpm 10.9.0。
- 状态：IN_PROGRESS。安装后执行 `pnpm build`、`node scripts/verify/sqlite-smoke.mjs`。
- 未完成：其余 K 阶段。

验证：pnpm install、pnpm build、SQLite smoke 均退出 0，状态 DONE。驱动安装期间首次提前运行缺 binding；等待安装并从官方 v12.4.1 Release 取得对应 Node v137 Windows 预编译包后重跑通过。
