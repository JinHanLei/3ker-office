# K10 — 全验收、CI与交接

目标：实际测试映射、随机指令不变量、架构方向、CLI/导出对账、独立数据验收、CI和交接；依赖 K00–K09；允许修改测试、验证工具、缺陷修复、文档。

实际命令：`pnpm install --frozen-lockfile`、`pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm verify`，均退出0。最终verify包含51个非空测试、2个实际CLI演示和验收映射检查。新增/改动代码已统一格式化；原任务文件未修改。

结果：离线 DONE；60/60内核PASS。数据9 PASS、G06 1 BLOCKED；没有把70项写成全部通过。源升级0.9.4、发现茅台精度冲突并隔离、取得替代五股真实包，真实交易演示成功。Windows本机实测，Linux/Windows CI配置存在但远端结果尚未观察。

修复：非法整数异常、测试glob误扫依赖、after-hours使用有效日规则、最后可交易边界错误优先级、存档运行时schema、不同actor账户隔离、缓存哈希检查。公开API与SQL不相互绕过。

证据：docs/verification/latest.md；tests/acceptance/cases.json；.runtime/verify-tests.json；.runtime/acceptance-report.json。
未完成：G06真实公司行为公告/实施审计、长历史源、备用适配器及历史制度覆盖，详见 docs/data/blockers.md。后续仅建议简单交易调试界面。
