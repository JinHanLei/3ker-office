# K08 — SQLite 恢复

目标：实际事务、CAS、迁移版本、幂等回执、新进程恢复和故障注入；依赖 K07；修改 adapters/persistence、recovery 测试和演示进程。
实际命令：`pnpm build`、`pnpm test:recovery`。
结果：8 项测试通过。Memory/SQLite 共用契约；beforeTransaction/afterRows/afterCommit 故障；CAS 拒绝；版本拒绝；三个真实子进程执行不中断/保存退出/新进程恢复，完整领域 SHA-256 相等。状态 DONE。
未完成：无。每次演示独立 .runtime/recovery-UUID；真实行情恢复仍待数据。
