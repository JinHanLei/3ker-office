# K03 — 场景和数据

目标：PIT、日历、校验和实际免费探针；依赖 K02；修改 core/features、adapters/data、scenarios、scripts/data。
命令：pnpm build；pnpm exec vitest run packages scripts/data/__tests__；pnpm data:probe -- --source baostock --start 2020-01-02 --end 2020-01-23。
结果：离线 7 项测试通过，离线 DONE；真实数据 BLOCKED。BaoStock 0.8.9 登录超时并返回 10002007，有限重试，原始错误保留于 .runtime/data/baostock。
未完成：真实数据覆盖、退市分钟和真实公司行为验证。规范化链已有代码，未被真实响应端到端验证。
