# K01 — 契约

目标：公共指令、场景、状态和错误码；依赖 K00；允许修改 contracts/docs。
预期：拒绝调用者倒填时间、浮点资金和缺少时区。
实际命令：`pnpm build`；`pnpm exec vitest run packages/contracts/src`。
结果：构建通过，契约测试 2 项通过。状态 DONE。
未完成：内核行为由后续阶段实现。首次测试发现 glob 包含依赖测试，已修正排除所有层级 node_modules。
