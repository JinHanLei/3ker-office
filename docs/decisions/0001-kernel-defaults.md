# ADR 0001 — 确定性默认政策

金额为整数分、价格为万分之一元、股份为整数；半入舍入。FIFO 成本，最后一份吸收余数。
采用 next-complete-bar-close-v1；bar 起点不得早于订单接收时刻。
累计成交额计算每订单最低佣金；所有账户共享测试容量。
毛股息应收桥接；送转总成本守恒；普通退出仅接受显式 game-writeoff-v1。
SQLite 使用整体版本快照和增量凭证/回执，CAS stateVersion。无数据库驱动进入 core。
允许合并简单功能文件，不生成空目录模板。真实规则不完整时明确使用 game-test 标签。
