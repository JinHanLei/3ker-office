# 场景格式

manifest/calendar/securities/names/bars-5m/trading-status/corporate-actions/rules/provenance/coverage-report JSON。整数值十进制字符串，时间带时区；加载时 Zod 结构校验、OHLC/会话/规则冲突校验与 SHA-256。数据缺口留给调度器在最后一致边界停止。真实包只能发布到忽略目录 .runtime，版本不可覆盖。
